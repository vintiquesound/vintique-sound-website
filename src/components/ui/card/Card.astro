---
interface Props {
  class?: string;
}
const { class: className = "" } = Astro.props;
---

<div
  data-slot="card"
  class={`flex flex-col py-6 bg-card text-card-foreground gap-6 rounded-xl border shadow-sm ${className}`}
>
  <slot />
</div>
