---
import WebPageLayout from "../layouts/WebPageLayout.astro";
import { Headphones, CheckCircle, GraduationCap, Package, ArrowRight } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";
import HeroSection from "@/layouts/sections/HeroSection.astro";

import HeroMixerImage from "../assets/website/audio_mixer-unsplash.jpg";
---

<WebPageLayout >
	<div class="flex flex-col">
    {/* Hero Section */}
		<HeroSection
			title="Professional Audio Services & More!"
			subtitle="From mixing & mastering, to educational content, to premium plugins, Vintique Sound helps you get professional quality music."
      primary={{ label: "Explore Services", href: "/mixing-mastering", icon: ArrowRight, iconPosition: "right" }}
      secondary={{ label: "View Tutorials", href: "/education" }}
			image={HeroMixerImage}
			imageAlt="Music Studio Audio Mixer"
			imagePosition="right"
		/>

    {/* Services Preview */}
    <section class="w-full py-20 bg-muted/50">
      <div class="mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8 xl:px-10 2xl:px-12">
        <div class="text-center mb-12">
          <h2 class="text-3xl md:text-4xl font-bold mb-4">Our Services</h2>
          <p class="text-lg text-muted-foreground max-w-2xl mx-auto">
            Professional audio services tailored to your needs
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <Card className="border-2 hover:border-primary transition-all duration-300">
            <CardHeader>
              <div class="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center mb-4">
                <Headphones className="h-6 w-6 text-primary" />
              </div>
              <CardTitle>Mixing & Mastering</CardTitle>
              <CardDescription>
                Professional mixing and mastering services to make your tracks shine
              </CardDescription>
            </CardHeader>
            <CardContent>
              <ul class="space-y-2 mb-6">
                <li class="flex items-center gap-2 text-sm">
                  <CheckCircle className="h-4 w-4 text-secondary" />
                  <span>Industry-standard quality</span>
                </li>
                <li class="flex items-center gap-2 text-sm">
                  <CheckCircle className="h-4 w-4 text-secondary" />
                  <span>Fast turnaround times</span>
                </li>
                <li class="flex items-center gap-2 text-sm">
                  <CheckCircle className="h-4 w-4 text-secondary" />
                  <span>Unlimited revisions</span>
                </li>
              </ul>
              <a href="/services" class="block">
                <Button fullWidth variant="default">
                  Learn More
                </Button>
              </a>
            </CardContent>
          </Card>

          <Card className="border-2 hover:border-secondary transition-all duration-300">
            <CardHeader>
              <div class="w-12 h-12 rounded-lg bg-secondary/10 flex items-center justify-center mb-4">
                <GraduationCap className="h-6 w-6 text-secondary" />
              </div>
              <CardTitle>Education & Tutorials</CardTitle>
              <CardDescription>
                Learn from industry professionals with our comprehensive tutorials
              </CardDescription>
            </CardHeader>
            <CardContent>
              <ul class="space-y-2 mb-6">
                <li class="flex items-center gap-2 text-sm">
                  <CheckCircle className="h-4 w-4 text-secondary" />
                  <span>Step-by-step courses</span>
                </li>
                <li class="flex items-center gap-2 text-sm">
                  <CheckCircle className="h-4 w-4 text-secondary" />
                  <span>Expert instructors</span>
                </li>
                <li class="flex items-center gap-2 text-sm">
                  <CheckCircle className="h-4 w-4 text-secondary" />
                  <span>Lifetime access</span>
                </li>
              </ul>
              <a href="/education" class="block">
                <Button fullWidth variant="secondary">
                  Browse Courses
                </Button>
              </a>
            </CardContent>
          </Card>

          <Card className="border-2 hover:border-accent transition-all duration-300">
            <CardHeader>
              <div class="w-12 h-12 rounded-lg bg-accent/10 flex items-center justify-center mb-4">
                <Package className="h-6 w-6 text-accent" />
              </div>
              <CardTitle>Digital Products</CardTitle>
              <CardDescription>
                Premium plugins, samples, and loops for your productions
              </CardDescription>
            </CardHeader>
            <CardContent>
              <ul class="space-y-2 mb-6">
                <li class="flex items-center gap-2 text-sm">
                  <CheckCircle className="h-4 w-4 text-secondary" />
                  <span>Professional quality</span>
                </li>
                <li class="flex items-center gap-2 text-sm">
                  <CheckCircle className="h-4 w-4 text-secondary" />
                  <span>Royalty-free content</span>
                </li>
                <li class="flex items-center gap-2 text-sm">
                  <CheckCircle className="h-4 w-4 text-secondary" />
                  <span>Instant download</span>
                </li>
              </ul>
              <a href="/products" class="block">
                <Button fullWidth variant="accent">
                  Shop Now
                </Button>
              </a>
            </CardContent>
          </Card>
        </div>
      </div>
    </section>

    {/* CTA Section */}
    <section class="w-full py-20 relative overflow-hidden">
      <div class="absolute inset-0 bg-linear-to-br from-primary via-primary/80 to-secondary" />
      <div class="mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8 xl:px-10 2xl:px-12 relative z-10 text-center">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">
          Ready to Elevate Your Sound?
        </h2>
        <p class="text-lg text-white/90 mb-8 max-w-2xl mx-auto">
          Join hundreds of producers and engineers who trust Vintique Sound for their audio needs
        </p>
        <div class="flex flex-wrap gap-4 justify-center">
          <Button size="lg" variant="secondary" className="bg-white text-primary hover:bg-white/90">
            Get Started Today
            <ArrowRight className="ml-2 h-5 w-5" />
          </Button>
          <Button size="lg" variant="outline" className="border-white text-black hover:bg-white/10">
            Contact Us
          </Button>
        </div>
      </div>
    </section>
  </div>
</WebPageLayout>
